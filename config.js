// ============================================================
// MyDailyTracler - Supabase configuration
// ------------------------------------------------------------
// 1. Go to https://supabase.com → create a project (free tier is fine).
// 2. Project Settings → API. Copy the Project URL and anon/public key.
// 3. Paste them below and save.
// 4. In the Supabase SQL editor, run the contents of supabase/schema.sql.
// 5. (Optional) In Auth → Providers, enable "Anonymous Sign-Ins" so users
//    don't need to register. Without it, the app falls back to localStorage.
// ============================================================

window.SUPABASE_CONFIG = {
  // Paste your Supabase project URL here, e.g. "https://xxxxx.supabase.co"
  url: "",

  // Paste your anon/public key here (starts with "eyJ...")
  anonKey: "",
};
